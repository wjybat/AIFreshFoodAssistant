# Copy to deploy/app.env on the Linux server and replace the placeholder.
LLM_API_KEY=replace-with-your-llm-api-key
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o
LLM_PROVIDER=auto

# Keep SERVER_URL as the externally reachable base URL used in QR codes.
SERVER_URL=http://49.232.193.14:8000